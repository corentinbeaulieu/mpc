# This file provides helpers needed by multiple pipelines
---
.load_pcvs:
  before_script:
    - source "$(ccc_home -u mpc)"/gitlab-runner/spack_organizer_inti/setup.sh
    - spack env activate mpc-runner-deps
    # Sanity check
    - pcvs --version
